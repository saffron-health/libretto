import { describe, test } from "vitest";

describe("Aff v2 input", () => {
  test.todo("parses positional arguments and named options from raw invoke input");
  test.todo("parses positional arguments and named options from an exec string");
  test.todo("surfaces input validation errors before middleware runs");
});
