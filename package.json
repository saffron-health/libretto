{
  "name": "libretto-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@ai-sdk/anthropic": "^3.0.58",
    "@ai-sdk/google": "^3.0.51",
    "@ai-sdk/google-vertex": "^4.0.80",
    "@ai-sdk/openai": "^3.0.41"
  },
  "scripts": {
    "build": "pnpm --filter libretto build",
    "type-check": "pnpm --filter libretto type-check",
    "test": "pnpm --filter libretto test",
    "eval": "pnpm evals",
    "evals": "pnpm --filter libretto build && pnpm --filter libretto-evals exec vitest run --config vitest.config.ts",
    "benchmarks": "pnpm --filter libretto build && pnpm --filter libretto-benchmarks exec tsx cli.ts",
    "cli": "pnpm --filter libretto cli"
  }
}
